{
  "name": "funpack-app",
  "private": true,
  "type": "module",
  "description": "Dev-only. Users never run npm: the app is served as plain ES modules with no build step.",
  "scripts": {
    "test": "node --test 'composer/tests/**/*.test.js'",
    "test:layout": "playwright test",
    "test:all": "npm test && npm run test:layout"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "jsdom": "^30.0.1"
  }
}
