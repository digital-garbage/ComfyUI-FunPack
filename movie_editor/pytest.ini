[pytest]
# Pin rootdir to movie_editor so pytest never climbs into the parent custom-node
# package (whose __init__ imports torch/comfy). importlib mode avoids package-walk.
testpaths = tests
addopts = --import-mode=importlib
