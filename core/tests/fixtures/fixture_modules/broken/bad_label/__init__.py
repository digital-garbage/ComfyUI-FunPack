"""A label that is not text. The UI would have to interpret it to show it."""

ID = "bad_label"
TITLE = "Bad label"
MOUNT = "generation.timing"
SETTINGS = {"enabled": {"type": "bool", "default": True, "label": {"markup": "<b>no</b>"}}}
